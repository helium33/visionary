# Hashed build assets never change, so browsers may keep them for a year.
# Everything else keeps Netlify's default of revalidating on every load.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
